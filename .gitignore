node_modules/
dist/
*.log

# Every .env variant (including backups like .env.bak.testnet, which hold real
# private keys) is excluded; only the committed template is allowed through.
.env*
!.env.example

# Build caches and local tooling state
.wrangler/
.claude/
.obsidian/
*.stackdump

# Personal notes, not part of the published project
What I Want To Know.md

# Secrets — never commit
cdp_wallet_secret.txt
cdp_api_key.json
*api*.txt
*secret*.txt
.dev.vars

# Internal planning/strategy docs — kept local, not published. These hold the
# competitive analysis, earnings projections and tax notes; a stray `git add -A`
# would otherwise publish them. The case variants matter: the pattern only
# matched on this machine because core.ignorecase is true on Windows.
Claude.md
CLAUDE.md
compass_artifact_*.md

# Obsidian scratch notes that live in this folder
Untitled.*
[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9].md
